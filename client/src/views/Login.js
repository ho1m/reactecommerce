import React from 'react';

const Login = () => {
  // const login = () => {
  //     try {history.push(history.location.state.from.pathname)}
  //     catch {history.push('/')}
  // }

  return (
    <div className="view">
      Login
    </div>
  );
}

export default Login;
